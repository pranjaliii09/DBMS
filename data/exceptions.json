[
  {
    "question": "1. Write a PL/SQL block to handle the NO_DATA_FOUND exception while fetching employee details.",
    "query": "DECLARE\n  v_name EMP.ENAME%TYPE;\nBEGIN\n  SELECT ENAME INTO v_name FROM EMP WHERE EMPNO = 9999;\n  DBMS_OUTPUT.PUT_LINE(v_name);\nEXCEPTION\n  WHEN NO_DATA_FOUND THEN\n    DBMS_OUTPUT.PUT_LINE('Error: Employee not found.');\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "Error: Employee not found." }
    ]
  },
  {
    "question": "2. Write a PL/SQL block to handle the TOO_MANY_ROWS exception.",
    "query": "DECLARE\n  v_name EMP.ENAME%TYPE;\nBEGIN\n  SELECT ENAME INTO v_name FROM EMP;\n  DBMS_OUTPUT.PUT_LINE(v_name);\nEXCEPTION\n  WHEN TOO_MANY_ROWS THEN\n    DBMS_OUTPUT.PUT_LINE('Error: Single row expected but multiple rows returned.');\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "Error: Single row expected but multiple rows returned." }
    ]
  },
  {
    "question": "3. Write a PL/SQL block to handle the ZERO_DIVIDE exception.",
    "query": "DECLARE\n  num1 NUMBER := 10;\n  num2 NUMBER := 0;\n  result NUMBER;\nBEGIN\n  result := num1 / num2;\n  DBMS_OUTPUT.PUT_LINE(result);\nEXCEPTION\n  WHEN ZERO_DIVIDE THEN\n    DBMS_OUTPUT.PUT_LINE('Error: Division by zero is not allowed.');\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "Error: Division by zero is not allowed." }
    ]
  },
  {
    "question": "4. Write a PL/SQL block to handle the VALUE_ERROR exception.",
    "query": "DECLARE\n  v_str VARCHAR2(2);\nBEGIN\n  v_str := 'HELLO';\n  DBMS_OUTPUT.PUT_LINE(v_str);\nEXCEPTION\n  WHEN VALUE_ERROR THEN\n    DBMS_OUTPUT.PUT_LINE('Error: Value too large for the variable size.');\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "Error: Value too large for the variable size." }
    ]
  },
  {
    "question": "5. Write a PL/SQL block with a generic WHEN OTHERS exception handler.",
    "query": "DECLARE\n  v_sal NUMBER;\nBEGIN\n  SELECT SAL INTO v_sal FROM EMP WHERE ENAME = 'KING';\n  v_sal := v_sal / 0;\nEXCEPTION\n  WHEN OTHERS THEN\n    DBMS_OUTPUT.PUT_LINE('An unexpected error occurred.');\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "An unexpected error occurred." }
    ]
  },
  {
    "question": "6. Write a PL/SQL block to display the error code and message using SQLCODE and SQLERRM.",
    "query": "DECLARE\n  v_num NUMBER;\nBEGIN\n  v_num := 'A';\nEXCEPTION\n  WHEN OTHERS THEN\n    DBMS_OUTPUT.PUT_LINE('Code: ' || SQLCODE);\n    DBMS_OUTPUT.PUT_LINE('Message: ' || SQLERRM);\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "Code: -6502" },
      { "SCRIPT_OUTPUT": "Message: ORA-06502: PL/SQL: numeric or value error: character to number conversion error" }
    ]
  },
  {
    "question": "7. Write a PL/SQL block to capture and display error details specifically when a division by zero occurs.",
    "query": "DECLARE\n  n NUMBER;\nBEGIN\n  n := 1/0;\nEXCEPTION\n  WHEN ZERO_DIVIDE THEN\n    DBMS_OUTPUT.PUT_LINE('Error Code: ' || SQLCODE);\n    DBMS_OUTPUT.PUT_LINE('Error Text: ' || SQLERRM);\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "Error Code: -1476" },
      { "SCRIPT_OUTPUT": "Error Text: ORA-01476: divisor is equal to zero" }
    ]
  },
  {
    "question": "8. Write a PL/SQL block to log an exception message into a variable and display it.",
    "query": "DECLARE\n  v_err_msg VARCHAR2(200);\nBEGIN\n  INSERT INTO EMP(EMPNO) VALUES(7839);\nEXCEPTION\n  WHEN OTHERS THEN\n    v_err_msg := SQLERRM;\n    DBMS_OUTPUT.PUT_LINE('Logged Message: ' || v_err_msg);\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "Logged Message: ORA-00001: unique constraint violated" }
    ]
  },
  {
    "question": "9. Write a PL/SQL block to raise an error if salary is less than 2000 using RAISE_APPLICATION_ERROR.",
    "query": "DECLARE\n  v_sal NUMBER := 1500;\nBEGIN\n  IF v_sal < 2000 THEN\n    RAISE_APPLICATION_ERROR(-20001, 'Salary must be at least 2000.');\n  END IF;\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "ORA-20001: Salary must be at least 2000." }
    ]
  },
  {
    "question": "10. Write a PL/SQL block to validate employee age and raise a custom error if age < 18.",
    "query": "DECLARE\n  v_age NUMBER := 16;\nBEGIN\n  IF v_age < 18 THEN\n    RAISE_APPLICATION_ERROR(-20002, 'Underage: Employees must be 18 or older.');\n  END IF;\nEND;",
    "output_table": [
      { "SCRIPT_OUTPUT": "ORA-20002: Underage: Employees must be 18 or older." }
    ]
  }
]